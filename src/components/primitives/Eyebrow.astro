---
/**
 * Monospace section label — "PROCESS", "WHY US?", "SELECTED BUILDS".
 */
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<p
  class:list={[
    'font-mono text-eyebrow uppercase text-navy',
    className,
  ]}
>
  <slot />
</p>
